# Rising Tide - the Drowned Covenant, the faction MLT founds when the Broken Coast accepts its invitation (event mltd.21,
# effect mltd_brk_join_the_covenant). Shaped like OWB's own templates (OWB_factions.txt). New file; OWB replace_paths the
# folder, so it needs load-after-OWB.
mltd_drowned_covenant_template = {
	name = mltd_drowned_covenant
	manifest = faction_manifest_conquest_of_territory
	goals = {
	}
	default_rules = {
		change_leader_rule_never
		call_to_war_rule_independent_only
		government_in_exile_allowed
	}
	icon = GFX_faction_logo_cascadia_pirates
	can_leader_join_other_factions = yes
}

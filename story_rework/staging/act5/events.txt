# ---- Round 27, Act V "The Sea Against Mars" (mltd_act5_focus.txt): mltd.50 opens the act, mltd.51 closes it with the
# pens of Flagstaff, and mltd.52 tells the world.

# Mars in the Water: the Legion's scouts find the Colorado running salt (fired by focus mltd_mars_in_the_water)
country_event = {
	immediate = { log = "[GetDateText]: [Root.GetName]: event mltd.50" }
	id = mltd.50
	title = mltd.50.t
	desc = mltd.50.d
	picture = GFX_event_CES_camp_2

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = mltd.50.a
	}
}

# When the Legion Breaks: the pens of Flagstaff - free the chained into the tide, or feed them to the spawning pools (fired
# by focus mltd_when_the_legion_breaks, which also grants mltd_mars_beneath_the_waves and previews both options with
# effect_tooltip: change the numbers in both places). The same 5,000 people either way: freed, they join the capital's
# population (+5 % stability); fed to the pools, they are 5,000 manpower at once. Option a merges the old The Chained Are
# Given to the Tide.
country_event = {
	immediate = { log = "[GetDateText]: [Root.GetName]: event mltd.51" }
	id = mltd.51
	title = mltd.51.t
	desc = mltd.51.d
	picture = GFX_event_legion_slaves

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = mltd.51.a
		ai_chance = {
			base = 2
		}
		capital_scope = {
			set_temp_variable = { pop_add = 5000 }
			add_state_population = yes
		}
		add_stability = 0.05
	}
	option = {
		name = mltd.51.b
		ai_chance = {
			base = 1
		}
		add_manpower = 5000
	}
}

# The Bull Goes Down to the Sea (world news; delivered by focus mltd_when_the_legion_breaks via every_other_country, so MLT
# never receives it)
news_event = {
	id = mltd.52
	title = mltd.52.t
	desc = mltd.52.d
	picture = GFX_event_CES_burning

	is_triggered_only = yes

	option = {
		name = mltd.52.a
	}
}
